version: 2

sources:
  - name: atomic
    schema: "{{ var('snowplow__atomic_schema', 'atomic') }}"
    tables:
      - name: events
        identifier: "{{ var('snowplow__events_table', 'events') }}"
  - name: derived
    schema: "{{ var('snowplow__page_views_schema', 'derived') }}"
    tables:
      - name: snowplow_web_page_views
        identifier: "{{ var('snowplow__page_views_table', 'snowplow_web_page_views') }}"